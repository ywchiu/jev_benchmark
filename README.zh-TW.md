# Jev Benchmark

[English](README.md) · **繁體中文**

五個決策模型在檢索增強助理的「路由」這一步做得如何 —— 而且是放在一整段對話裡看，不是只看
單一個問題。

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="results/charts/leaderboard-dark.svg">
  <img alt="各系統的路由決策正確率" src="results/charts/leaderboard-light.svg" width="760">
</picture>

## 為什麼有這份評測

TypeSafe 推出了 Jev，一個回傳「有型別的答案」而不是文字的託管模型 —— 你給它一個情況和一串選
項，它回你其中一個選項，外加一個機率。幾天之內就出現了做同樣事情的開源專案：有的直接從凍結
的 Qwen 模型裡讀選項機率，有的用 Google 的擴散模型做，還有一個是專門為這件事訓練的 322M
編碼器。

對任何準備拿這個來蓋東西的人來說，接下來的問題很明顯。託管那個每次呼叫要錢，而且資料會送到
別人那裡。開源那些跑在自己的機器上。它們真的可以互相替換嗎？如果不行，差別到底出現在哪裡？

沒有人在一個有真實結構的任務上量過，所以我們量了。

## 這是什麼場景

一個公司內部的助理，要從自己的資料裡回答問題：六個知識庫，涵蓋制度、產品、銷售、專案與法
務；八份具體文件，像是已簽的合約、一份八十頁的手冊；還有六個工具，可以讀 ERP、讀 CRM、讀行
事曆，或開一張工單。

這就是檢索增強生成（RAG），而這份測試量的是**回答之前**的那一步。有人問了一個問題，總得有
東西決定答案要從哪裡來。需要開一份文件、查一個知識庫、上網找、還是查資料庫？或者答案其實已
經在對話裡了？這一步錯了，後面全部都錯：助理會從錯的來源回答，或是什麼都沒取就自己編。

在單一問題的示範裡這很簡單。放進真實對話就不是了，因為地基會一直動。使用者說「算了，就看這
份文件」，這個限制接下來好幾輪都要守住。兩輪之後他整個換了話題，舊的限制就該解除。然後他又
說「順便查一下現行折扣政策」—— 那是新話題，還是同一件事多加一個來源？

這份測試就是一百個這樣的時刻：二十段對話，每段五輪。每一輪都附上它之前發生過的事，所以五個
系統面對的情況完全一樣。過程中不會真的去取任何資料，也不會真的呼叫工具 —— 評的是決策，不是
答案。

## 每個數字是什麼意思

每一輪，系統要同時做對四件事。

**答案要去哪裡找**（`route`）—— 九個答案裡選一個：用對話裡現有的內容、用一般常識回答、開一
份指定的文件、查知識庫、上公開網路找、呼叫工具、同時用上面其中兩類、跟使用者問清楚缺少的資
訊，或因為不被允許而拒絕。

**話題是不是換了**（`scope change`）—— 多輪之所以難，就難在這一題。六個答案：什麼都沒動、使
用者換了話題、同一件事現在多需要一個來源、使用者把範圍收窄了、目前的來源之間有衝突所以該先
問再動作，或是這個請求被擋下。這一題答錯，就是使用者明明說「只看這個檔」，助理卻跑去翻整個
知識庫。

**要怎麼處理**（`mode`）—— 直接回答、改寫或摘要、分析比較、追問，或擋下。

**之後受什麼限制**（`restriction`）—— 下一輪是可以自由挑來源、只能用指定的那幾份文件，還是
不准碰公開網路。這一項會延續：它一輪一輪傳下去，直到使用者解除為止，所以這裡出錯會悄悄污染
後面每一輪。

下面表格裡的**決策正確**，指的是這四件事在同一輪全部答對。

## 結果

| 系統 | 決策正確 | 去哪裡找 | 話題是否換了 | 怎麼處理 | 之後的限制 | p50 | p95 |
|---|---|---|---|---|---|---|---|
| **Gemma 4 31B**（自建） | **77.0%** | 90.0% | 87.0% | 92.0% | **95.0%** | 2,293 ms | 4,723 ms |
| **Jev 1.13.0**（託管 API） | 61.4% | **90.6%** | 85.8% | 91.6% | 84.4% | **749 ms** | **818 ms** |
| djev-spark（自建） | 32.2% | 69.2% | 51.0% | 80.2% | 90.4% | 1,230 ms | 1,470 ms |
| SemIf（自建） | 24.0% | 62.0% | 49.0% | 65.0% | 91.0% | 627 ms | 1,206 ms |
| Laya 322M（自建） | 0.0% | 18.0% | 25.0% | 10.0% | 36.0% | **189 ms** | 312 ms |

p50 是一般情況下的回應時間，p95 是慢的那一端 —— 二十次裡有十九次會比它快。

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="results/charts/decisions-zh-dark.svg">
  <img alt="四個決策中的三個逐項比較" src="results/charts/decisions-zh-light.svg" width="900">
</picture>

四個決策裡的三個，放在同一個刻度上。「要怎麼處理」沒有放進來，因為前兩名在那一項差不到半個
百分點，分不出什麼。這張圖清楚顯示的是：Gemma 和 Jev 在前兩根一樣高，到第三根才分家。

完整表格，包含這份測試自己那套更嚴格的計分，在 [`results/summary.md`](results/summary.md)。

## 這些數字說了什麼

**Gemma 和 Jev 在決策上很接近，差別在記性。** 兩者「去哪裡找」的選擇差不到半個百分點，「話題
是否換了」的判斷差大約一個百分點。幾乎整個差距都在限制狀態這一項：95% 對 84%。因為這個欄位
會往後傳，在這裡出錯會污染整段對話後面的部分 —— 這也是為什麼 Gemma 答對的完整對話數大約是
Jev 的兩倍。

**Jev 是可預測的那一個。** 它慢的那一端只比平常慢一點點，818 毫秒對 749 毫秒。Gemma 慢的那
一端超過平常的兩倍，而且超過四秒。如果你承諾的是回應時間而不是回報平均值，這件事比準確率的
差距更要緊。

**沒有人知道什麼時候該停下來問。** 當請求真的缺了助理需要的東西，正確的做法是開口問。Gemma
有 43% 的時候做對，Jev 是 31%，而且兩者錯的方式一樣：不問，直接去呼叫工具。如果那個工具有
副作用 —— 開工單、把訊息寄出去 —— 路由模型就不能是它前面唯一的把關者。

**五個裡面有兩個不該用在這件事上。** Laya 快得非常多，但沒有答對過；它自己的說明文件就寫
著，基礎模型在這類任務上不微調的話接近亂猜。SemIf 一次都沒有認出該因為權限而拒絕的請求 ——
十五次機會，零次抓到。

## 在數字有意義之前必須先修好的東西

過程中有五個量測上的毛病產生了嚴重錯誤的結論，而且大部分從分數本身根本看不出來。最糟的那個
讓某個系統拿了 3.8 分，實際上應該是 32.2 分：我們的轉接程式把中文用跳脫字元送出去，讓輸入長
度變成五倍，而題目的數量剛好跨過一個門檻，把伺服器推進一個會壞掉的回答格式。另一個則讓整輪
變成零分，因為 SSH 通道斷線，而連線失敗在計分上和答錯完全一樣。

完整記錄在 [`docs/method.md`](docs/method.md)。簡短版是：當一個系統分數很難看，先確認它真的
讀到了輸入，再相信那個數字。

## 這個倉庫裡有什麼

| 路徑 | 內容 |
|---|---|
| [`questions/`](questions/) | 每個系統被問的十一道題，以及為什麼這樣拆 |
| [`results/summary.md`](results/summary.md) | 完整結果表 |
| [`results/per_system/`](results/per_system/) | 每個系統每一輪的預測、計時與評分 |
| [`results/charts/`](results/charts/) | 上面那些圖表 |
| [`docs/method.md`](docs/method.md) | 測試怎麼跑的，以及哪些地方會出錯 |
| [`docs/gold-audit.md`](docs/gold-audit.md) | 標準答案的稽核，在跑任何系統之前完成 |
| [`code/`](code/) | 轉接程式與計分程式 |

## 值得知道的限制

這是照腳本回放，不是真的 agent 環境。系統走錯路時不會有分支，所以在這裡分數高不代表它能完成
真實任務。標準答案是合成的，也沒有經過第三方覆核；我們的稽核找到一個真的有爭議的標籤，以及
一條標準答案有在遵守、但從來沒寫出來的規則。每段對話只有五輪，離記憶真正開始出問題的長度還
很遠。自建系統的時間數字是和其他工作共用同一張 GPU 量到的，因此偏保守。
